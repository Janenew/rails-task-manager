
    <div class="margin">
    <%= link_to "<i class=\"glyphicon glyphicon-pencil\">Edit this task</i>".html_safe, edit_task_path(@task), class: "btn btn-default pull-right"  %>
    </div>

    <h1> <%= @task.title %> </h1>



    <input type="text" value="<%= @task.details %>" disabled class="form-control">
    <br>
    <input type="checkbox" <%= 'checked' if @task.completed %> >
    <%= @task.completed ? 'This task is completed' : 'This task is not completed yet' %>
    <br>
  <%= link_to "<i class=\"glyphicon glyphicon-step-backward\"></i>".html_safe + " Back to list", tasks_path %>

